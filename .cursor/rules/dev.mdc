---
description: 本地开发启动方式与前端端口约定
globs:
  - "**/*"
---

- 在本仓库中启动开发服务时，应在**终端前台**运行（例如 `npm run dev`），**不要**使用后台运行，以便在终端里直接看到日志与报错。
- 前端开发服务器端口**固定为 18001**，不要修改 `client/vite.config.ts` 中的 `server.port`；已启用 `strictPort`，若端口被占用会失败而非自动换端口——请先结束占用 18001 的进程再启动。
- 后端 API 端口为 **3001**，一般无需改动。
- 后端 CORS 默认仅允许本机 Vite（18001）与 `vite preview`（4173）；跨机调试可设置环境变量 `LITERATURE_API_CORS_ORIGINS`（逗号分隔），详见根目录 `README.md`。
